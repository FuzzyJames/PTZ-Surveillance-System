apiVersion: v1
kind: Secret
metadata:
  name: surveillance-secrets
  namespace: surveillance
type: Opaque
stringData:
  # NOTE: In production, use sealed-secrets or external secret manager.
  # These values should be replaced before deploying.
  CAMERA_PASSWORD: "REPLACE_ME"
